export type ClientApiItem = {
  id: string;
  name: string;
  registration_date: string;
}

export type ClientUiItem = {
  id: string;
  name: string;
  registrationDate: string;
}

export type ClientEditableProps = {
  name: string;
}